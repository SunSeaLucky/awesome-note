---
title: LLM 插件
date: 2025-11-06 14:22:00 +0800
categories: [科研, LLM 插件]
tags: [llm, research]     # TAG names should always be lowercase
---

## coding

```markdown
When writing code:
1. Do not add comments.
2. The code must be as reusable as possible.
3. The code must remain concise.
4. Do not output excessive information; only output the most critical information.
5. Delete test file after testing.
```

## pipeline

```markdown
1. Dot NOT ANY write summary or document unless I order you to do so.
```

## proxy

```markdown
1. We are in Chinese mainland. Set `PORT=7884 && export http_proxy=http://127.0.0.1:$PORT && export https_proxy=http://127.0.0.1:$PORT && export HTTP_PROXY=http://127.0.0.1:$PORT && export HTTPS_RPOXY=http://127.0.0.1:$PORT` in your terminal to use proxy IF YOU ENCOUNTER NETWORK ISSUES. I ran a proxy server at that port.
```